<Page x:Class="AegisPC.App.Views.NetworkView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="{Binding PageTitle}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Style="{StaticResource PageHeaderStyle}" Text="Ağ İzleyici ve Güvenlik" />

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0" PlaceholderText="Süreç adı, IP veya Port ile filtrele..." 
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                        Icon="{ui:SymbolIcon Search24}" Margin="0,0,12,0"/>

            <ui:Button Grid.Column="1" Content="Yenile" Icon="{ui:SymbolIcon ArrowClockwise24}" 
                       Command="{Binding LoadConnectionsCommand}"/>
        </Grid>

        <!-- Connections Table -->
        <ui:DataGrid Grid.Row="2" ItemsSource="{Binding Connections}" 
                     AutoGenerateColumns="False" IsReadOnly="True"
                     SelectionMode="Single">
            <ui:DataGrid.Columns>
                <DataGridTextColumn Header="Protokol" Binding="{Binding Protocol}" Width="80"/>
                <DataGridTextColumn Header="Süreç" Binding="{Binding ProcessName}" Width="160"/>
                <DataGridTextColumn Header="PID" Binding="{Binding PID}" Width="70"/>
                <DataGridTextColumn Header="Yerel Adres" Binding="{Binding LocalAddress}" Width="140"/>
                <DataGridTextColumn Header="Yerel Port" Binding="{Binding LocalPort}" Width="90"/>
                <DataGridTextColumn Header="Uzak Adres" Binding="{Binding RemoteAddress}" Width="140"/>
                <DataGridTextColumn Header="Uzak Port" Binding="{Binding RemotePort}" Width="90"/>
                <DataGridTextColumn Header="Durum" Binding="{Binding State}" Width="110"/>
                <DataGridTextColumn Header="Yol" Binding="{Binding ProcessPath}" Width="*"/>
            </ui:DataGrid.Columns>
        </ui:DataGrid>

        <!-- Status Bar -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Gray" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</Page>
