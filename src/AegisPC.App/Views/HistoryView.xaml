<Page x:Class="AegisPC.App.Views.HistoryView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:conv="clr-namespace:AegisPC.App.Converters"
      Title="{Binding PageTitle}"
      Background="#EEF2F6"
      UseLayoutRounding="True" SnapsToDevicePixels="True"
      TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Style="{StaticResource PageHeaderStyle}" Text="Uygulama ve Güvenlik Denetim Günlüğü" />

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0" PlaceholderText="Eylem, hedef veya detay ile filtrele..." 
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                        Icon="{ui:SymbolIcon Search24}" Margin="0,0,12,0"/>

            <ui:Button Grid.Column="1" Content="Yenile" Icon="{ui:SymbolIcon ArrowClockwise24}" 
                       Command="{Binding LoadLogsCommand}"/>
        </Grid>

        <!-- Logs DataGrid -->
        <Grid Grid.Row="2">
            <ui:DataGrid ItemsSource="{Binding Logs}" 
                         AutoGenerateColumns="False" IsReadOnly="True"
                         SelectionMode="Single">
                <ui:DataGrid.Columns>
                    <DataGridTextColumn Header="Zaman" Binding="{Binding Timestamp, StringFormat='\{0:yyyy-MM-dd HH:mm:ss\}'}" Width="160"/>
                    <DataGridTextColumn Header="Eylem" Binding="{Binding Action}" Width="150"/>
                    <DataGridTextColumn Header="Hedef Türü" Binding="{Binding TargetType}" Width="110"/>
                    <DataGridTextColumn Header="Hedef Adı" Binding="{Binding TargetName}" Width="180"/>
                    <DataGridTextColumn Header="Sonuç" Binding="{Binding Result}" Width="90"/>
                    <DataGridTextColumn Header="Detaylar" Binding="{Binding Details}" Width="*"/>
                </ui:DataGrid.Columns>
            </ui:DataGrid>

            <!-- Empty State Overlay -->
            <Border Background="#EEF2F6" Visibility="{Binding HasNoLogs, Converter={StaticResource BoolToVis}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock Text="Henüz kaydedilmiş aktivite bulunmuyor" FontSize="18" FontWeight="SemiBold" 
                               HorizontalAlignment="Center" Foreground="#0F172A"/>
                    <TextBlock Text="Taramalar, karantina işlemleri ve güvenlik olayları burada listelenir." 
                               FontSize="13" Foreground="#475569" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Gray" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</Page>
