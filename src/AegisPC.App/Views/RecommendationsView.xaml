<Page x:Class="AegisPC.App.Views.RecommendationsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:conv="clr-namespace:AegisPC.App.Converters"
      Title="{Binding PageTitle}">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:RiskLevelToColorConverter x:Key="RiskColorConverter"/>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Style="{StaticResource PageHeaderStyle}" Text="Sistem İyileştirme ve Güvenlik Tavsiyeleri" />

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Sisteminizin sağlığını, hızını ve gizliliğini korumak için önerilen adımlar:" Foreground="Gray" VerticalAlignment="Center"/>
            <ui:Button HorizontalAlignment="Right" Content="Yenile" Icon="{ui:SymbolIcon ArrowClockwise24}" 
                       Command="{Binding LoadRecommendationsCommand}"/>
        </Grid>

        <!-- Content Area -->
        <Grid Grid.Row="2">
            <!-- Cards List -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Recommendations}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource MetricCardStyle}" Margin="0,0,0,12" Padding="16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Left Content -->
                                    <StackPanel Grid.Column="0">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                            <Border Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                                                <TextBlock Text="{Binding Category}" FontSize="11" FontWeight="Bold" Foreground="#2563EB"/>
                                            </Border>
                                            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource BrushTextPrimary}"/>
                                        </StackPanel>

                                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{DynamicResource BrushTextSecondary}" FontSize="13" Margin="0,2,0,6"/>

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Neden önemli: " Foreground="{DynamicResource BrushTextMuted}" FontSize="12" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Reasoning}" TextWrapping="Wrap" Foreground="#D97706" FontSize="12"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Action Buttons -->
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                                        <ui:Button Content="Uygula" Appearance="Primary" Margin="0,0,8,0"
                                                   Command="{Binding DataContext.ApplyRecommendationCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                   CommandParameter="{Binding}"/>
                                        <ui:Button Content="Yoksay" Appearance="Secondary"
                                                   Command="{Binding DataContext.DismissRecommendationCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                   CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Empty State Overlay -->
            <Border Background="{DynamicResource BrushAppBg}" Visibility="{Binding HasNoRecommendations, Converter={StaticResource BoolToVis}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="48" Foreground="{DynamicResource BrushStatusSafe}" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock Text="Sisteminiz optimize durumda!" FontSize="18" FontWeight="SemiBold" 
                               HorizontalAlignment="Center" Foreground="{DynamicResource BrushTextPrimary}"/>
                    <TextBlock Text="Şu anda bekleyen herhangi bir optimizasyon veya güvenlik önerisi bulunmuyor." 
                               FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Gray" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</Page>

