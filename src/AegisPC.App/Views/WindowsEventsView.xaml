<Page x:Class="AegisPC.App.Views.WindowsEventsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:conv="clr-namespace:AegisPC.App.Converters"
      Title="{Binding PageTitle}">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Style="{StaticResource PageHeaderStyle}" Text="Windows Sistem ve Uygulama Olayları" />

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" ItemsSource="{Binding Channels}" SelectedItem="{Binding SelectedChannel}" Margin="0,0,12,0" Height="36"/>

            <ui:TextBox Grid.Column="1" PlaceholderText="Kaynak, Event ID veya hata içeriği ile ara..." 
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                        Icon="{ui:SymbolIcon Search24}" Margin="0,0,12,0"/>

            <ui:Button Grid.Column="2" Content="Yenile" Icon="{ui:SymbolIcon ArrowClockwise24}" 
                       Command="{Binding LoadEventsCommand}"/>
        </Grid>

        <!-- Events Grid + Detail Panel -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="340"/>
            </Grid.ColumnDefinitions>

            <!-- Table -->
            <ui:DataGrid Grid.Column="0" ItemsSource="{Binding Events}" 
                         SelectedItem="{Binding SelectedEvent}"
                         AutoGenerateColumns="False" IsReadOnly="True"
                         SelectionMode="Single" Margin="0,0,16,0">
                <ui:DataGrid.Columns>
                    <DataGridTextColumn Header="Zaman" Binding="{Binding TimeCreated, StringFormat='\{0:yyyy-MM-dd HH:mm:ss\}'}" Width="150"/>
                    <DataGridTextColumn Header="Kanal" Binding="{Binding LogName}" Width="100"/>
                    <DataGridTextColumn Header="Seviye" Binding="{Binding Level}" Width="90"/>
                    <DataGridTextColumn Header="ID" Binding="{Binding EventId}" Width="70"/>
                    <DataGridTextColumn Header="Kaynak (Provider)" Binding="{Binding ProviderName}" Width="160"/>
                    <DataGridTextColumn Header="Mesaj" Binding="{Binding Message}" Width="*"/>
                </ui:DataGrid.Columns>
            </ui:DataGrid>

            <!-- Detail Panel -->
            <Border Grid.Column="1" Style="{StaticResource MetricCardStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <TextBlock Text="Olay Detayları" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>

                        <StackPanel Visibility="{Binding SelectedEvent, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{Binding SelectedEvent.ProviderName}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding SelectedEvent.EventId, StringFormat='Event ID: \{0\}'}" Foreground="Gray" Margin="0,0,0,8"/>

                            <TextBlock Text="Kanal ve Seviye" FontWeight="SemiBold" Margin="0,8,0,4"/>
                            <TextBlock Text="{Binding SelectedEvent.LogName, StringFormat='Kanal: \{0\}'}" Margin="0,2"/>
                            <TextBlock Text="{Binding SelectedEvent.Level, StringFormat='Seviye: \{0\}'}" Margin="0,2"/>

                            <TextBlock Text="Zaman" FontWeight="SemiBold" Margin="0,8,0,4"/>
                            <TextBlock Text="{Binding SelectedEvent.TimeCreated, StringFormat='\{0:yyyy-MM-dd HH:mm:ss\}'}" Margin="0,2"/>

                            <TextBlock Text="Açıklama" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" Margin="0,12,0,4"/>
                            <Border Background="#F1F5F9" BorderBrush="{DynamicResource BrushCardBorder}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,4">
                                <TextBlock Text="{Binding SelectedEvent.Message}" TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource BrushTextPrimary}"/>
                            </Border>

                            <TextBlock Text="Ham XML" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextPrimary}" Margin="0,12,0,4"/>
                            <Border Background="{DynamicResource BrushAppBg}" BorderBrush="{DynamicResource BrushCardBorder}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,4" MaxHeight="200">
                                <ScrollViewer>
                                    <TextBlock Text="{Binding SelectedEvent.RawXml}" TextWrapping="Wrap" FontSize="11" FontFamily="Consolas" Foreground="{DynamicResource BrushTextSecondary}"/>
                                </ScrollViewer>
                            </Border>
                        </StackPanel>

                        <TextBlock Text="Ayrıntıları görmek için bir olay seçin." Foreground="Gray" HorizontalAlignment="Center" Margin="0,40,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedEvent}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Gray" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</Page>

